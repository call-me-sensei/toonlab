# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
.toonlab/

# Private test assets — licensed content that must never enter the public
# repo. The labs discover models/environments dropped in here (see
# docs/characters.md). Back this folder up — it is not in git.
assets-local/

# Licensed reference texture — supply your own painted stone tile; the
# outdoor-world example runs without it (cliffs just skip the triplanar
# detail layer).
examples/outdoor-world/stone.png

# Generated local-asset manifests (npm run assets:local)
labs/shared/localCharacterAssets.generated.js
labs/shared/localEnvironmentAssets.generated.js

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
