// Headless-safe Character Shader defaults. Keep these separate from the
// Vite-discovered asset catalog so stores and contract tests can run in Node.
export const DEFAULT_MODEL_URL = '/characters/mannequin.glb';
