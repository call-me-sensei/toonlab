// Level D intentionally runs the exact public reference implementation.
// The clean-consumer Vite config permits this one host-authored source file;
// every ToonLab import inside it still resolves from the packed dependency.
import '../../examples/walkable-reference/main.js';
