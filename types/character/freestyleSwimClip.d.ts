export function createFreestyleSwimClip(targetMesh: any, rig: any, { clipName, trackNameStyle, }?: {
    clipName?: string;
    trackNameStyle?: string;
}): THREE.AnimationClip;
import * as THREE from 'three';
